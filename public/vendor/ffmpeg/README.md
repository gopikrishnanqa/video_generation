FFmpeg.wasm binaries for in-browser MP4 encoding.

Copied from `@ffmpeg/ffmpeg` and `@ffmpeg/core` on `npm install` via `scripts/copy-ffmpeg-vendor.js`.

Do not edit manually unless upgrading versions.
