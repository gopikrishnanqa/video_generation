# Media assets

| Folder | Contents |
|--------|----------|
| `music/` | Background MP3 and WAV tracks for generated videos |
| `reference-videos/` | Example MP4 outputs for pacing reference |

Poster images remain in `/samples`.

Add new MP3 files to `music/` and list them in `lib/media-catalog.ts`.
